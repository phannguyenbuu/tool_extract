import { SVGShapeData } from './types';

// Default Sheet Dimensions in mm
export const DEFAULT_SHEET_WIDTH = 420;
export const DEFAULT_SHEET_HEIGHT = 594;

// Empty initial shapes, user will upload them
export const SHAPES: SVGShapeData[] = [];